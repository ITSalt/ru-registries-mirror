# Зависимостей нет: всё на стандартной библиотеке Python 3.10+.
# XLSX читается через zipfile + xml.etree — чтобы зеркало и скилл ставились
# на машины без pip и python3-venv, где `pip install` просто не отработает.
